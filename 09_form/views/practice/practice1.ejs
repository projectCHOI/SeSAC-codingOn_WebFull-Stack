<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습1</title>
  </head>
  <body>
    <h1>실습1</h1>
    <form action="/practice1/result" method="get">
      <label>이름: <input type="text" name="name" required /></label>
      <br />
      <label>성별:</label>
      <label
        ><input type="radio" name="gender" value="남자" required /> 남자</label
      >
      <label
        ><input type="radio" name="gender" value="여자" required /> 여자</label
      >
      <br />

      <!-- 생년월일 선택 -->
      <label>생년월일:</label>
      <select name="year">
        <% for (let i = 1980; i <= 2024; i++) { %>
        <option value="<%= i %>"><%= i %></option>
        <% } %>
      </select>
      년
      <select name="month">
        <% for (let i = 1; i <= 12; i++) { %>
        <option value="<%= i %>"><%= i %></option>
        <% } %>
      </select>
      월
      <select name="day">
        <% for (let i = 1; i <= 31; i++) { %>
        <option value="<%= i %>"><%= i %></option>
        <% } %>
      </select>
      일
      <br />

      <!-- 관심사 선택 -->
      <label>
        <input type="checkbox" name="interest" value="여행" /> 여행
      </label>
      <label>
        <input type="checkbox" name="interest" value="패션" /> 패션
      </label>
      <label>
        <input type="checkbox" name="interest" value="음식" /> 음식
      </label>
      <br />

      <!-- 좋아하는 색상 -->
      <label>좋아하는 색상: <input type="color" name="color" /></label>
      <br />

      <!-- 좋아하는 숫자 -->
      <label>
        좋아하는 숫자:
        <input type="number" name="number" max="1000" />
      </label>
      <br />

      <!-- 제출 버튼 -->
      <button type="submit">회원가입</button>
    </form>
  </body>
</html>
